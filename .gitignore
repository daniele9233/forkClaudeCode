# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
dist-ssr/
target/
src-tauri/target/

# Tauri
src-tauri/gen/

# Vite / TS
*.local
*.tsbuildinfo

# Env files (never commit secrets)
.env
.env.local
.env.*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# OpenCode server password (if ever stored locally)
opencode.env

# OpenCode runtime artifacts written into the sidecar's working directory
# (src-tauri during dev). These hold local provider keys — never commit them.
src-tauri/config.json
src-tauri/opencode.json
src-tauri/auth.json
src-tauri/.opencode/
src-tauri/storage/
