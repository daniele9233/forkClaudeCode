# FORGIA — Roadmap & Checklist

> Documento operativo, accanto a `PROGETTO.md`. Lo stato delle spunte **è** la nostra memoria tra le sessioni.
>
> **Protocollo:** a inizio sessione → leggi il Log + trova la prossima `[ ]`. A fine sessione → spunta il fatto + 2–3 righe nel Log.
>
> **Architettura (promemoria):** GUI Tauri (React/TS) **sopra** `opencode serve` (sidecar). Non costruiamo il motore. Vedi `PROGETTO.md` §4–§5.

---

## 📌 Log di sessione

> Voce più recente in cima. Formato: `data — fatto — scoperto — riprendere da`.

- _2026-07-04 — **Ricette: iniezione FULL skill (12.39).** Le ricette iniettavano solo 2 skill (cap matcher) → per awwwards-caliber non basta. Ora `planInjection(text, forcedIds)` forza TUTTE le skill della ricetta (bypass cap, source "recipe", non-sticky); `composer.fill` porta gli skillIds → `ChatInput.recipeSkillIds` → `SendOpts.forcedSkillIds` → `handleSend`. Base awwwards: ogni ricetta forza anche impeccable+type-color+emil-motion → 5–7 playbook per brief. 53 test verdi. **Riprendere da:** verifica in-app (lancia ricetta → chip "will apply" con 5-7 skill)._
- _2026-07-04 — **Fix composer (12.38): Perfeziona off su ricette + resize libero.** (1) `composer.fill(text, source)`; ricette → `source:"recipe"` → bottone **Perfeziona disabilitato** (tooltip "già ottimizzata"), riabilitato svuotando il campo. (2) Tolto `max-h-[50vh]` che bloccava l'ingrandimento: auto-grow fino 60vh + maniglia manuale fino 85vh e giù al minimo; `ResizeObserver` rileva il drag e ferma l'auto-grow. (3) Caricando una ricetta si azzera il resize manuale → la maniglia funziona subito. 52 test verdi. **Riprendere da:** verifica in-app (ricetta → Perfeziona disabilitato + drag resize su/giù)._
- _2026-07-04 — **Studio: 5 ricette scroll-media/video (12.37).** La skill `scroll-media` non era in nessuna ricetta → aggiunte 5 ricette (28→33): Product Reveal (AirPods), Cinematic Scroll Video (scrubbing), Frame-by-frame Story, Real Video Landing, Fashion Lookbook Reel. 52 test verdi._
- _2026-07-04 — **Asset realistici + video/scroll + pipeline frame (12.36).** Tutte e 3 in sequenza. (1) Asset: `asset-generation` con regola dura no-404/no-grigio → foto reali via **Lorem Picsum** (no chiave) + SVG inline; direttiva Web Designer aggiornata; **auto-heal** in anteprima (INSPECTOR_JS: `<img>` 404 → Picsum al volo). (2) Skill **scroll-media** (🎞️: image-sequence canvas AirPods-style, video scrubbing/scroll-controlled, background video reale). (3) Skill **video-pipeline** (🎥: genera start/end→video se c'è un gen tool, altrimenti stock reale; estrae 100–300 frame con ffmpeg + WebP; wiring). Catalogo 25→27. 52 test verdi. **Scoperto:** higgsfield è a crediti/legato alla dev-session → le skill usano "gen tool se presente", altrimenti Picsum/stock/ffmpeg. **Riprendere da:** verifica in-app (auto-heal preview; serve ffmpeg per l'estrazione frame)._
- _2026-07-04 — **Indicatore visione modello 👁 (12.35).** In base al modello selezionato si vede se "vede le immagini": `modelCaps.ts` (`modelSupportsVision` via `modalities.input`/`attachment` + hook `useModelVision`). ModelSwitcher: icona 👁/EyeOff nel chip + per riga nel menu + riga scritta in fondo ("Audit e stile da URL/screenshot funzionano/non funzioneranno"). Scheda Stili: riga sotto l'import che riflette il modello attuale. 50 test verdi. **Riprendere da:** eventuale stile per singola sezione._
- _2026-07-04 — **Style Memory: import da URL/screenshot (12.34).** Nella scheda Stili, box "Importa uno stile": da **URL** (screenshot via `capture_preview` + HTML via `fetch_text`, il modello con visione usa lo screenshot, quello testuale l'HTML) o da **immagine** (dialog nativo). `style.ts` refactor `runDistiller` + `captureStyleFromUrl`/`captureStyleFromImage`; `NO_STYLE` → errore chiaro. Zero nuovo Rust. 50 test verdi. **Riprendere da:** verifica in-app con un URL reale (serve modello vision per il top); prossimo eventuale: stile per singola sezione._
- _2026-07-04 — **Style Memory 🎨 (12.33): salva uno stile e riusalo.** Bottone **🎨 Stile** nell'anteprima → `captureStyle()` (sessione nascosta plan-mode, IBRIDO: DESIGN.md se c'è, altrimenti distillato dal codice — niente visione) → salva in libreria persistente (`styles.store`). Lo stile **attivo** è iniettato nel ruolo **system** a ogni invio ("build strictly to this saved design system"). Nuova scheda **Stili** (rinomina/usa/elimina/DESIGN.md espandibile) + chip 🎨 nel composer con ✕. Riusabile tra progetti diversi. 50 test verdi. **Riprendere da:** verifica in-app (crea sito → 🎨 Stile → nuovo progetto → attiva stile → costruisci)._
- _2026-07-04 — **Skill engine v2 (12.32): sticky, slash+pin, system-role, semantic, negative/exclusion.** Le 5 aree del feedback tecnico. (1) **Sticky skills** (store `sticky`+`noteActivated`, 3 turni) → risolve il "secondo prompt". (2) **Slash** (`/motion …` forza una skill) + **chip cliccabili** per pin/unpin (pinnate = tutta la sessione) + bottone **reset**. (3) **Iniezione nel ruolo `system`** (SDK `session.prompt` ha `system`): playbook+direttive nel system, user message pulito, chip via id-tag nascosti. (4) **Matching ibrido**: mappa sinonimi + campo `phrases`. (5) **negativeKeywords** ("senza animazioni" sopprime motion) + **excludes** (neubrutalism⟂minimalism). Nuove fn in match.ts: `resolveSlash`/`planInjection`/`buildSkillSystem`/`tagSkills`. +8 test, 47 totali. **Riprendere da:** verifica in-app (chip cliccabili, sticky al 2° prompt, /comando); eventuale Brief Builder/Clarify-first._
- _2026-07-04 — **Prompt Enhancer ✨ (12.31) — il fix del problema "prompt".** Bottone **Perfeziona** nella chat: riscrive la richiesta grezza in un brief esperto (stile+layout+sezioni+brand+stack+qualità) prima di inviare, editabile. `enhance.ts` usa una sessione nascosta plan-mode (come il distillatore di memoria, `[kikko]`, silenziata, cancellata) col modello selezionato; `memory.ts` espone markSilent/unmarkSilent. Human-in-the-loop (rivedi e invia). 39 test verdi. **Riprendere da:** verifica in-app (scrivi un prompt vago → Perfeziona → vedi il brief); eventuale toggle "auto-perfeziona" o Brief Builder/Clarify-first come step 2._
- _2026-07-04 — **Skill Motion Craft di Emil Kowalski (12.30).** Nuova skill `emil-motion` (🎞️, da emilkowalski/skills · animations.dev): il playbook condensato di design-engineering per le animazioni (quando animare, easing/mai ease-in su UI + curve custom, durate <300ms, mai scale(0), transizioni vs keyframes, timing asimmetrico, stagger, performance solo transform/opacity, a11y reduced-motion). Resa skill nativa (iniezione prompt), non dipende dalla CLI `skills`. Catalogo 24→25. 39 test verdi. **Riprendere da:** verifica in-app (Skills → cerca "animation"/"emil")._
- _2026-07-04 — **Skill DESIGN.md + 4 ricette spec-driven (12.29).** Nuova skill `design-md` (📋, da voltagent/awesome-design-md): scrivi prima un DESIGN.md a 9 sezioni, poi costruisci attenendoti. Catalogo 23→24. +4 ricette enterprise che la usano (24→28): Design System First, Living Style Guide, Productivity SaaS, Media/Consumer Tech. 39 test verdi. **Riprendere da:** verifica in-app (Studio → nuove card, Skills → cerca "design.md")._
- _2026-07-04 — **Asset reali + QA multi-viewport & a11y (12.28).** Due leve scelte dall'utente. (1) **Asset reali:** skill `asset-generation` (🖼️: image-gen MCP se c'è, altrimenti SVG/gradienti/grana procedurali + favicon + OG via @vercel/og + pipeline AVIF/WebP/srcset/lazy/blur-up); direttiva Web Designer aggiornata. Catalogo 22→23. (2) **QA:** `capture_preview` ora accetta width/height (Rust) → bottone **Audit** cattura a 390/768/1440 e manda le 3 viste all'agente con checklist mobile-first; nuovo **A11y radar** automatico (auditor in INSPECTOR_JS: lang/alt/nomi/label/CONTRASTO/heading/tap-target) con drawer + "Fix with agent" (`qa.store`). 39 test verdi. **Scoperto:** cargo check qui è bloccato dalla network policy (registry) → Rust lo compila la CI; verificato per struttura. **Riprendere da:** verifica su Windows di Audit multi-viewport (serve modello vision) e A11y scan._
- _2026-07-04 — **Studio +14 ricette enterprise (12.27).** Da 10 a 24 ricette: 14 verticali ENTERPRISE, siti full-stack di livello vendibile ($10k+) — Fintech/Neobank, Cybersecurity, HealthTech, Luxury Real Estate, Law Firm, D2C E-commerce, Web3/Crypto, Enterprise AI, Dev Tool/API, Award Agency, Luxury Hospitality, Corporate B2B, Analytics, Education/LMS. Nuovo campo `category` + preambolo/helper `enterprise()` che impone multi-pagina con routing, conversione (form, social proof, trust), SEO, design-system, performance/a11y/deploy. Scheda Studio raggruppata in "Enterprise" + "Stili" con conteggi. Test aggiornato (≥20 + almeno una enterprise). 37 test verdi. **Riprendere da:** verifica su Windows delle ricette enterprise (routing multi-pagina + build)._
- _2026-07-04 — **Agente web più esperto (12.26): audit design + taste packs + Web Designer mode + registry.** 4 leve scelte dall'utente. (1) **Design Review automatico:** bottone **Audit** nell'anteprima (screenshot → checklist Impeccable a 7 aree con voti /10 → applica i fix); la 📷 resta come review veloce. (2) **Taste packs:** skill `type-color` (🎨) con abbinamenti font reali (mai Inter/Arial) + palette accessibili con hex. (3) **Registry componenti:** skill `component-registry` (📦: shadcn/Aceternity/Magic UI/React Bits). (4) **Web Designer mode** sempre attivo (default on): `injectWebDesigner` prepende una direttiva da team front-end a OGNI prompt web (marker nascosto), toggle nella scheda Studio. Catalogo 20→22 skill. 37 test verdi. **Scoperto:** audit/📷 richiedono un modello con visione (deepseek-chat non vede immagini). **Riprendere da:** verifica su Windows del bottone Audit con un modello vision e del toggle Web Designer._
- _2026-07-04 — **Studio full-stack + watchdog anti-loop (12.25).** (1) Le ricette Studio ora generano un PROGETTO front-end reale (Vite+React19+TS+Tailwind, Framer Motion, GSAP+ScrollTrigger, Lenis, CSS scroll-driven, 3D con React Three Fiber/drei/Spline + .glb ottimizzati, `npm run dev`) invece di un index.html statico: preambolo `STACK` condiviso + nuova skill `creative-3d` (🧊). (2) Fix "pensa pensa ma non scrive nulla / loop": in MessageBubble i puntini "thinking…" restano finché non c'è contenuto visibile e le parti reasoning/text vuote (tipiche dei reasoning model e del free tier GLM) non lasciano più un bubble bianco freezato; nuovo watchdog di stallo (`chat.store.lastActivityAt` + `useStallSeconds` + `StallBanner`) che dopo 25s di silenzio mostra un banner + Stop. 34 test verdi. **Scoperto:** il sintomo aveva 2 cause (UI su parti vuote + run appeso), coperte insieme. **Riprendere da:** verifica su Windows delle ricette full-stack (scaffold + 3D) e del banner di stallo._
- _2026-07-04 — **Studio: 10 ricette di web design + skill top-market (12.24).** L'agente è ora specializzato in siti web professionali. Catalogo skill esteso da 12 a 19: `impeccable` (anti-pattern del design AI generico: no Inter/Arial, no grigio su colore, no nero puro, no card annidate, no easing elastico — da pbakaus/impeccable), `neubrutalism`, `neumorphism` (con fix a11y), `skeuomorphism`, `minimalism` editoriale, `web-layouts` (F/Z-shape, split-screen, asimmetrico, masonry/Pinterest), `web-architect` (sito multi-sezione completo). Nuovo `src/skills/recipes.ts`: 10 briefing pronti stile+layout (Bento SaaS, Glass Aurora AI, Neubrutalist Agency, Soft Neumorphic, Editorial Minimal, Skeuomorphic Product, Pinterest Gallery, Split Duotone, Z-Pattern Startup, Immersive Scroll). Nuova scheda **Studio** in Settings (default) con card cliccabili: `composer.store` inietta il brief nella chat + spiegazione di cosa sono le skill. 34 test verdi (+4 recipes). **Scoperto:** i brief self-contained non dipendono dal matcher (cap 2 skill); i keyword nel brief attivano comunque le skill giuste. **Riprendere da:** verifica su Windows dell'aspetto delle ricette; eventuale import automatico del repo impeccable completo via MCP._
- _2026-07-03 — **ModelSwitcher: stato "online" verde.** Il modello agganciato ora si evidenzia in verde (pallino glow + nome verde nell'header, riga attiva nel dropdown con pallino + nome semibold + tag ONLINE + check). Selezione via `model.store` (per-prompt param) che vince sulla config del motore pinnata da plugin auth. **Riprendere da:** richiesta siti web professionali._
- _2026-07-02 — **Memoria di progetto 🧠 (12.21) + Rules/import (12.22).** Memoria = blocco marcato in AGENTS.md (iniezione nativa del motore, merge deterministico Rust, regole umane intoccate, versionata col repo); distillazione su idle con sessione nascosta plan-mode (digest 9K → memoria 4K, sezioni fisse), throttle ≥4 msg/≥3 min, Memorize now + toggle in Settings→Rules; sessioni `[kikko]` nascoste e silenziate; fix leak live-messages cross-sessione in MessageList. Rules tab (editor AGENTS.md) + import skill da URL (fetch_text Rust + parser md → custom persistite, catalogo dinamico). **Riprendere da:** verifica su Windows (4 comandi Rust nuovi); prossimo: 12.23 Test Radar._
- _2026-07-02 — **Fase 12 COMPLETA: kill processi (12.18) + perf chat (12.19) + Autopilot (12.20).** `process::kill_child_tree` (taskkill /T /F) per Sidecar e DevRunner; MessageBubble memo + WeakMap parts stabili (in streaming si ridisegna solo il bubble attivo); Autopilot: toggle Auto nell'input ($ budget, × iter), controller su idle (marker AUTOPILOT_DONE, fuel gauge dai cost del motore, cap), AutopilotBar con stop, coda in attesa durante il run. **Riprendere da:** verifica completa su Windows (Rust toccato). Resta solo 11.4 macOS/Linux (futuro)._
- _2026-07-02 — **Review per file (12.13) + coda di task (12.14) + notifiche (12.15).** `ReviewPanel` collassabile in chat: file toccati (git status motore) con A/M/D e +/−, click→diff Monaco, Discard 2-step (`discard_file_changes`: checkout HEAD / delete per nuovi). Coda: invio durante il run accoda (`queue.store` per sessione), drain automatico a idle con guardia prev-running, chip rimovibili sopra l'input. Notifiche `tauri-plugin-notification` solo a finestra non a fuoco. **Riprendere da:** verifica su Windows (Rust: comando discard + plugin notification). Restano: 12.18 kill processi, 12.19 virtualizzazione chat, 12.20 autopilot, 11.4 macOS/Linux._
- _2026-07-02 — **HMR nel pannello (12.17) + l'agente vede la sua pagina (12.16).** WS della pagina ridirezionati dritti al dev server dallo script iniettato (`__kikko_ws_target__` + monkey-patch `WebSocket`) → HMR Vite/Next vivo nell'anteprima, live-edit senza reload. Bottone 📷: `capture_preview` (Edge/Chrome headless di sistema, timeout 45s) → PNG allegato via `useSendPrompt.files` a un prompt di critica visiva. **Riprendere da:** verifica su Windows (Rust toccato); serve modello con visione per il 📷; prossime: 12.13 review-diff / 12.14 coda task / 12.15 notifiche._
- _2026-07-02 — **Checklist riallineata al lavoro reale:** spuntate D2–D5 e 1.2 (decisioni prese/superate), nuova **Fase 12** con tutto il post-roadmap di luglio (12.1–12.12 fatte) e le prossime candidate (12.13–12.20). **Riprendere da:** scegliere la prossima tra 12.13 (review-diff), 12.14 (coda task), 12.15 (notifiche)._
- _2026-07-02 — **Affidabilità + Error Radar ⭐.** (1) SSE auto-reconnect con backoff (stream caduto ≠ app sorda); (2) memoria chat riconciliata su idle (refetch → clearSession, niente crescita infinita); (3) auto-scroll solo se sei già al fondo. **Error Radar:** lo script iniettato dal proxy cattura errori runtime della pagina (js/promise/resource/console.error) → badge ⚠ sulla toolbar anteprima + drawer + "Fix with agent" che manda gli errori all'agente in un click. +8 test sul matcher skills (22→30). **Riprendere da:** verifica su Windows (ricompilare: Rust toccato); prossime feature candidate: review-diff con approvazione per file, coda di task, notifiche desktop, screenshot→self-critique._
- _2026-07-01 — **Project/workspace picker (fondamentale).** Aggiunta la scelta della cartella su cui lavorare: in opencode il "progetto" è la cwd di `opencode serve`, finora mai impostata. **Rust:** `sidecar.working_dir` (→ `current_dir` nello spawn); comandi `get_working_dir`/`set_working_dir` (imposta cwd + riavvia + salva last-project), `clone_repo` (git clone di sistema → private via credenziali locali), `create_project` (mkdir + git init opz.); `tauri-plugin-dialog` + `dialog:default`; `config_store::save/load_last_project` (`kikkocode.json`) → riapre l'ultimo progetto all'avvio. **Frontend:** `workspace.store` (currentDir + recents persistiti), `useProjectActions` (dialog nativo + openProject che ri-initClient sulla nuova porta, reset sessione, invalidate), `ProjectPicker` (3 tab: Open folder / Clone from GitHub / New project + recenti), `ProjectBar` in cima alla sidebar. **Riprendere da:** gestione token per private repo e switch-progetto dalla command palette._
- _2026-07-01 — **UX: pannello inferiore ridimensionabile + tasto anteprima in-app.** (1) Il prompt non si poteva abbassare quando Terminal/Inspector/Timeline erano aperti (pannello `h-[42vh]` fisso): ora il divisore è una maniglia trascinabile, `ui.store.bottomHeight` (def 340) + `App.tsx startResize` (pointer su document, `innerHeight-clientY-28`, clamp `[140, innerHeight*0.82]`). (2) Nuovo tasto `MonitorPlay` in `ChatShell` che apre l'anteprima **in-app** (iframe `PreviewPanel`, colonna destra) invece di Chrome: toggle `openPreview(detectedUrl ?? localhost:5173)` / `closePreview()`. Lint/build/22 test verdi, prettier pulito. **Riprendere da:** conferma su Windows del drag-resize e dell'anteprima in-app._
- _2026-06-30 — **Fix connessione motore (Windows):** l'app restava su "CONNECTING TO ENGINE…" pur con un `opencode serve` (1.17.11) attivo a mano su :4096. Cause + fix in `src-tauri/src/sidecar/mod.rs`: (1) `opencode_bin`+`which_on_path` ora risolvono lo shim npm su Windows scandendo `PATH` con `PATHEXT` (`Command::new("opencode")` non lo applica → spawn fallito); (2) nuovo `engine_command` instrada gli shim `.cmd`/`.bat` via `cmd.exe /C` (gli `.exe`/Unix restano diretti; il bundle release usa `opencode.exe` reale → produzione pulita); (3) escape hatch `OPENCODE_BASE_URL` per agganciarsi a un server avviato a mano; (4) stdio ereditato + log `[kikkocode] …` per vedere la causa reale nel terminale. Inoltre `version.ts`: SDK (0.x) e server (1.x) hanno versioni indipendenti → il vecchio check `==0.15` nagava sempre; ora avvisa solo se major motore `< 1` (`MIN_ENGINE_MAJOR`). Lint verde, 22 test ok (Rust → CI). **Riprendere da:** confermare su Windows che l'app si collega (auto-spawn o via `OPENCODE_BASE_URL`); poi eventuale orphan-kill dello shim `cmd /C` su restart._
- _2026-06-27 — **Fix CI:** il job Rust falliva (`resource path binaries/opencode-x86_64-unknown-linux-gnu doesn't exist`): Tauri valida `externalBin` a check-time e il binario non è committato. Spostato `externalBin` dal `tauri.conf.json` base a un overlay `src-tauri/tauri.release.conf.json` mergiato solo in release (`tauri build --config src-tauri/tauri.release.conf.json` in `release.yml`). Aggiornati ADR 06 + binaries/README. JSON validati. (Frontend CI già verde.) **Riprendere da:** verificare che il job Rust torni verde su questo push._
- _2026-06-26 — **Fatto:** Fase 11.1–11.3 (11.4 macOS/Linux = futuro) — Packaging & release. **11.1** sidecar bundling: `externalBin` + `opencode_bin()` (bundlato accanto all'exe, fallback PATH), `binaries/` README+gitignore, `release.yml` Windows (tauri-action), ADR D4. **11.2** onboarding primo avvio: `onboarding.store` (flag persistito) + `OnboardingWizard` 3-step (welcome/provider-setup/tour) mostrato quando engine ready + "Replay Intro" nella palette. **11.3** `README.md` + `CHANGELOG.md`. Build verde, 22 test ok, prettier pulito. **Scoperto:** Tauri externalBin vuole il suffisso target-triple (`opencode-<triple>[.exe]`) e a runtime lo mette accanto all'eseguibile senza suffisso. **Riprendere da:** roadmap principale completa (Fasi 1–11 salvo 11.4 macOS/Linux). Prossimi possibili: verificare il bundle reale su Windows con un opencode scaricato, rifinire l'asset-name del fetch in release.yml, code-split del bundle (>500KB)._
- _2026-06-26 — **Fatto:** Fase 10 COMPLETA (10.1–10.4) — Hardening. **10.3** resilienza sidecar (Rust): `start` con retry su porta fresca (TOCTOU/porte occupate), `restart` + `restart_opencode` command, `spawn_health_monitor` (poll /health, emette `opencode-error`) con generation-guard; frontend `restartSidecar()`. **10.2** `SidecarStatusBanner` globale (Connecting / Reconnect su crash). **10.1** audit a11y: reduced-motion completo (CSS @media + `useReducedMotion()`), `role="dialog"` sugli overlay, aria-label. **10.4** Vitest+jsdom, 22 test sul layer d'integrazione (chat/permission/theme store, detectDevServer, events), step `pnpm test` in CI. **Scoperto:** in web-env la network policy blocca `static.crates.io` → `cargo check` impossibile qui (solo `index.crates.io` permesso); Rust verificato per ispezione, lo compila la CI. **Riprendere da:** Fase 11 — Packaging & release (11.1 bundle binario opencode + installer Windows, 11.2 onboarding primo avvio, 11.3 README/release notes)._
- _2026-06-26 — **Fatto:** Fase 9 COMPLETA (9.1–9.3) — Pass estetico. `stores/theme.store.ts` (dark/light persistito, no-FOUC via import in `main.tsx`) + `features/settings/ThemeToggle.tsx` (Sun/Moon nell'header). `index.css`: `.light` completato (sidebar/ring/destructive/ombre) + scrollbar light-aware. `features/onboarding/WelcomeScreen.tsx`: empty state firmato con anvil+forge-glow ambra (Motion), stagger reveal, 3 suggestion-chip che prefillano un prompt. `App.tsx`: fade-in d'apertura una-tantum. `MessageList.tsx`: reveal fade-up dei bubble (key stabile → no re-anima in streaming). **D3:** niente Magic UI/Aceternity come dipendenze — effetti hand-rolled con Motion per controllare `prefers-reduced-motion` ovunque. Build verde 1001KB. **Scoperto:** `useReducedMotion()` di Motion va usato per disabilitare gli effetti a livello di componente (il `@media` CSS non copre le animazioni JS di Motion). **Riprendere da:** Fase 10 — Hardening (10.1 reduced-motion audit, 10.2 stati errore/empty, 10.3 resilienza sidecar, 10.4 test integrazione)._
- _2026-06-26 — **Fatto:** Fase 8 COMPLETA (8.1–8.3) — Differenziatori UX. `session.ts`: `useRevertSession` + `useUnrevertSession` (SDK session.revert/unrevert). `features/checkpoints/CheckpointTimeline.tsx`: timeline verticale con dot amber al punto revert, banner "Session reverted" con pulsante Restore, elementi post-revert con opacity-40. `features/commandpalette/CommandPalette.tsx`: overlay `Ctrl+K`, 7 azioni + 12 sessioni recenti, ricerca fuzzy label/desc/group, nav ↑↓ Enter Esc. `features/statusbar/StatusBar.tsx`: strip 24px sempre visibile; progress bar context window color-codificata (65%/85%); costo sessione cumulato; pill provider/model. `App.tsx`: tab "Timeline", `<StatusBar />` sempre visibile, overlay `CommandPalette`+`SettingsModal` globali, listener Ctrl+K. `ui.store.ts`: aggiunto `commandPaletteOpen`, `settingsOpen`, `openSettings`, `closeSettings`. Build verde 872KB. **Scoperto:** `session?.revert?.messageID` indica il punto revert corrente; usare `liveMessages` dal chat store per integrare i messaggi live con quelli storici (stesso pattern di ContextInspectorPanel). **Riprendere da:** Fase 9 — Pass estetico._
- _2026-06-26 — **Fatto:** Fase 7 COMPLETA (7.1–7.4) — Funzioni motore nella GUI. `opencode/config.ts`: `useConfig`, `useUpdateConfig`, `useSetAuth`, `useAgents`, `useMcpStatus`, `useSessionChildren`. `features/settings/ModelSwitcher.tsx`: dropdown provider+modelli nell'header (pill compatto, context-K label, chiave API inline). `features/settings/SettingsModal.tsx`: modal con tab Skills (lista agenti da /agent con badge mode/built-in/tools) e tab MCP (lista server da config.mcp + status da /mcp, toggle enabled, remove, add local/remote). `ChatShell.tsx`: gear icon → SettingsModal, ModelSwitcher nel header. `session.ts`: `useSessionChildren`. `SessionSidebar.tsx`: `SubagentList` (children indentati con icona Bot). `useChatEvents.ts`: handler `session.created` per aggiornare lista + children in tempo reale. Build verde 857KB. **Scoperto:** `Agent.tools` è `{[name]: boolean}`, `McpStatusResponses[200]` è `unknown` (typed a run-time). No API enable/disable agente da SDK. **Riprendere da:** Fase 8 — Differenziatori UX._
- _2026-06-26 — **Fatto:** Fase 6 COMPLETA (6.1–6.5) — Context Inspector. `opencode/context.ts`: `useContextMessages(sessionId)` (raw fetch → `/api/session/{id}/context` non esposto dall'SDK), `useProviders()` (`getClient().config.providers()` → Array<Provider> con model.limit.context), `contextKeys` factory, `useContextInvalidate()`. `useChatEvents.ts`: aggiunto handler `EventSessionCompacted` → invalida context cache. `ui.store.ts`: aggiunto `"inspector"` a `BottomTab`. `features/inspector/ContextInspectorPanel.tsx`: merge storico+live AssistantMessage per aggregare token/cost; progress meter colore-codificato (verde/amber/rosso); 3 sezioni: Context Window (% bar con fmtNum, colore per soglie 65/85%), Session Totals (6-cella griglia: input/output/reasoning/cache-r/cache-w/cost), In Context (breakdown ruoli dai context messages con barre proporzionali). `App.tsx`: tab "Inspector" sempre disponibile nel bottom panel (non condizionale su selectedFilePath). Build verde 841KB. **Scoperto:** `Session` type SDK non ha tokens/cost; i dati vivono in `AssistantMessage.tokens` (per step) + `StepFinishPart`; il context endpoint non è nell'SDK → raw fetch. **Riprendere da:** Fase 7 — Funzioni motore nella GUI._
- _2026-06-26 — **Fatto:** Fase 5 COMPLETA (5.1–5.5) — Selezione visuale degli elementi. Architettura: Vite plugin (`ForgiaInspectorPlugin.ts`, `forgiaInspector()`) iniettato nell'HTML del progetto utente via `transformIndexHtml`; script bridge naviga l'albero fiber React (`__reactFiber$*` → `_debugSource`) per mappare DOM→file:riga in dev mode (zero Babel transform extra necessario grazie a `@babel/plugin-transform-react-jsx-source` già attivo in `@vitejs/plugin-react`); fallback a `data-forgia-loc` attribute. Comunicazione bidirezionale via `window.parent.postMessage`/`window.addEventListener('message')`: forgia:enable/disable (da GUI), forgia:hover/select/ready/pong (da iframe). `stores/selection.store.ts`: stato selectionMode, hoveredElement, selectedElement, inspectorReady, composeText. `PreviewPanel.tsx` aggiornato: pulsante Crosshair con glow amber quando attivo; hint "aggiungi il plugin" se inspector non rilevato; onLoad ping→pong handshake; `useEffect` listenMessage+cleanup; selectionMode ref stabile per evitare re-register. `ElementCompose.tsx`: pannello compatto (tag+file:riga, HTML troncato, open-in-editor, testo→prompt → `useSendPrompt`). ADR `docs/05-adr-visual-selection.md`. Build verde 836KB. **Scoperto:** React in dev mode già traccia `_debugSource` — nessun plugin Babel separato necessario. Loop completo: click → compose → OpenCode edita → HMR ricarica iframe → vedi risultato. D1 deciso: fiber+postMessage. **Riprendere da:** Fase 6 — Context Inspector._
- _2026-06-26 — **Fatto:** Fase 4.3 — Anteprima web (Fase 4 COMPLETA). `features/preview/PreviewPanel.tsx`: `<iframe>` verso `previewUrl` con toolbar (reload via `reloadKey`, barra URL editabile con normalizzazione schema, open-external `window.open`, close); HMR gestito dal dev server stesso (ricarica l'iframe internamente). `App.tsx`: terza colonna a destra (`w-1/2 border-l`) montata se `previewUrl != null`. ADR `docs/04-adr-web-preview.md`: scelto iframe per ora (testabile senza Rust, dev server non bloccano embedding), webview WRY nativa rimandata a Fase 5 per la selezione visuale (l'iframe cross-origin verso localhost non permette injection diretta). Build verde. **Scoperto:** la selezione visuale (Fase 5) richiederà script iniettato nel progetto (plugin) o webview WRY con `initialization_script` per bypassare la same-origin. **Riprendere da:** Fase 5 — Selezione visuale degli elementi (5.1 studio Onlook)._
- _2026-06-26 — **Fatto:** Fase 4.2 — Rilevamento dev server. `features/terminal/detectDevServer.ts`: `detectDevServerUrl(text)` con regex per `http(s)://localhost|127.0.0.1|0.0.0.0[:port]` + bare `localhost:port`, normalizza 0.0.0.0→localhost (testato su output Vite/Next/CRA). `stores/preview.store.ts`: `detectedUrl`, `previewUrl`, `dismissed` + azioni. `useTerminalEvents`: scansiona output (completed/error) e `state.metadata` (running, per dev server long-running che non si completano mai) → `setDetectedUrl`. `features/preview/DevServerBanner.tsx`: banner ambra con URL + "Open preview" (→ `openPreview`, usato in 4.3) + dismiss; montato in ChatShell sotto l'header. Build verde. **Scoperto:** i dev server restano in stato `running` (non `completed`), quindi la detection deve guardare anche `metadata`. **Riprendere da:** Fase 4.3 — Anteprima web in webview Tauri._
- _2026-06-26 — **Fatto:** Fase 4.1 — Terminale xterm.js. Installati `@xterm/xterm@6`, `@xterm/addon-fit`, `@xterm/addon-web-links`. `stores/terminal.store.ts`: `entries: TermEntry[]` + `writtenIds: Set` per dedupe per part.id. `stores/ui.store.ts`: `bottomOpen` + `bottomTab` ('terminal'|'diff') + `toggleTerminal`. `features/terminal/useTerminalEvents.ts`: hook montato in ChatShell, capta `message.part.updated` con tool=bash su completed/error e aggiunge entry (command da `state.input.command`). `TerminalPanel.tsx`: xterm read-only (`disableStdin`), tema forge (bg #0a0c0e, cursor amber), ANSI (cyan `$ cmd`, rosso errori), FitAddon+ResizeObserver, scrive entry incrementalmente, tasto Clear. `App.tsx`: bottom panel con tab-bar Terminal/Diff (diff solo se file aperto), terminale resta montato (display:hidden) per preservare scrollback; effect riporta a terminal se il file viene chiuso. `ChatShell`: toggle terminale nell'header. `file.store.openFile` ora apre il bottom panel su tab diff. `FileTree` usa `openFile`. Build verde (826KB — xterm pesante, lazy-split in Fase 10). **Riprendere da:** Fase 4.2 — Rilevamento dev server da output._
- _2026-06-26 — **Fatto:** Fase 3.3 — Apertura file alla riga esatta. `file.store.ts`: aggiunto `selectedLine: number|null` e `openFile(path, line?)` (atomico). `FileDiffPanel.tsx`: `revealLine(editor, monaco, line, decoRef)` con `editor.revealLineInCenter + setPosition + deltaDecorations` (classe `monaco-target-line`); `handleEditorMount`/`handleDiffMount` rivelano la riga al mount; `useEffect([selectedLine])` re-rivela se cambia dopo il mount; header mostra `path:riga`; fallback a `patch.hunks[0].newStart` se nessuna riga esplicita. `ToolCallCard.tsx`: `extractFileRef(input)` (prova chiavi `path/filePath/file_path/file` + `line/startLine/start_line/lineNumber`), chip `ExternalLink basename:N` cliccabile → `openFile`. `monaco-editor@0.55.1` installato come devDep per i tipi. Build verde (487KB). **Riprendere da:** Fase 4 — Terminale xterm.js._
- _2026-06-26 — **Fatto:** Fase 3.2 — Diff inline Monaco. `@monaco-editor/react@4.7.0` installato. `src/stores/file.store.ts`: Zustand store `selectedFilePath`. `FileDiffPanel.tsx`: header con path+badge git (A/M/D, +N/-N linee), Monaco DiffEditor side-by-side per file con patch, Monaco Editor read-only per file puliti, `reverseApplyPatch` che ricostruisce l'originale dai patch hunks (logica: context copy+modIdx++, `-` → origLines, `+` → modIdx++). Pannello `h-[42vh]` sotto la chat, si mostra solo quando `selectedFilePath != null`. `FileTree.tsx`: selection ora usa store via `setSelectedFilePath`. Build verde (485KB). **Riprendere da:** Fase 3.3 — Apertura file alla riga esatta._
- _2026-06-26 — **Fatto:** Fase 3.1 — File tree del progetto. `src/opencode/file.ts`: hooks TanStack Query per `useProjectCurrent`, `useFileList(path)`, `useFileRead`, `useFileStatus`, `useInvalidateFiles`. `src/features/filetree/FileTree.tsx`: tree ricorsivo con lazy-load per directory, badge git A/M/D colorati (verde/amber/rosso), icone per estensione (TS=blu, RS=arancio, JSON=giallo, MD=grigio, CSS=rosa, config=viola), sort dirs-first, filtro file ignored. Sottoscrizione a `file.edited` e `file.watcher.updated` per invalidare la cache. `SessionSidebar.tsx`: rimossi `w-56 shrink-0 border-r` dall'aside (spostati al parent). `App.tsx`: sidebar sinistra `w-64` con sessions (max 45% altezza) + divider + file tree (flex-1). Build verde (467KB). **Riprendere da:** Fase 3.2 — Diff inline Monaco._
- _2026-06-26 — **Fatto:** polish UI — angoli prompt input arrotondati elegantemente. `ChatInput.tsx`: `rounded-xl` → `rounded-2xl` + `overflow-hidden` sul container esterno; children ora vengono clippati dentro i bordi. Build verde. **Riprendere da:** Fase 3 — Editor & diff (file tree + Monaco)._
- _2026-06-26 — **Fatto:** Fase 2.5 — Gestione sessioni sidebar. `useDeleteSession` aggiunto a `session.ts` (DELETE /session/{id}). `SessionSidebar.tsx`: lista sessioni ordinata per updated, running indicator pulsante, tempo relativo, tasto New (+), delete on hover (Trash2, non visibile se running). Auto-switch alla sessione precedente dopo delete. `App.tsx` → layout 2-colonne (sidebar 224px + chat flex). `pnpm build` verde (459KB). **Riprendere da:** Fase 3 — Editor & diff (file tree + Monaco)._
- _2026-06-26 — **Fatto:** Fase 2.4 — Input prompt + toggle Plan/Build. `session.ts` → `useSendPrompt` ora accetta `agent?: string` (passato alla body del prompt). `ChatInput.tsx` ristrutturato: riga superiore con pill-toggle Build/Plan (icone Hammer/Map, default Build, bloccato durante run), placeholder dinamico col mode attivo. `ChatShell.tsx` → `handleSend` ora riceve `(text, mode)` e passa `agent: mode` alla mutation. `pnpm build` verde (456KB). **Scoperto:** `agent` nel body del prompt è stringa libera (`"plan"` | `"build"` | `"general"`); `AssistantMessage.mode` restituisce il mode usato. **Riprendere da:** Fase 2.5 — Gestione sessioni nella sidebar._
- _2026-06-26 — **Fatto:** Fasi 2.2–2.3. 2.2: ToolCallCard (già in 2.1) spuntata — card generica con icone per tool type, expand input/output, stati pending/running/completed/error. 2.3: HITL completo — `src/opencode/permission.ts` (useRespondPermission: once/always/reject), `src/stores/permission.store.ts` (Zustand+persist: Map pending + Set allowList con custom storage per Set), `useChatEvents.ts` aggiornato (permission.updated → auto-approve se in allowList, altrimenti addPending), `PermissionBanner.tsx` (banner ambra con 3 pulsanti: Once/Always/Reject; Always → salva tipo in allowList). ChatShell aggiornato con banner sopra l'input. `pnpm build` verde (454KB). **Scoperto:** SDK method `postSessionIdPermissionsPermissionId` (non sotto `session.`); response = "once"|"always"|"reject". **Riprendere da:** Fase 2.4 — Input prompt + toggle Plan/Build._
- _2026-06-26 — **Fatto:** Fase 2.1 — Chat shell + streaming markdown. Creati: `src/stores/chat.store.ts` (liveParts/liveMessages/runningSessions), `src/opencode/useChatEvents.ts` (SSE→store, 6 event types con explicit type params), `src/features/chat/MarkdownContent.tsx` (ReactMarkdown+remarkGfm, streaming cursor), `ToolCallCard.tsx` (ToolPart: pending/running/completed/error, collapsible input+output), `MessageBubble.tsx` (user bubble right, assistant bubble left con text/reasoning/tool), `MessageList.tsx` (auto-scroll, merge live+historic parts), `ChatInput.tsx` (textarea auto-resize, Enter=send, Shift+Enter=newline, abort button), `ChatShell.tsx` (wiring completo, auto-create session). `App.tsx` → solo `<ChatShell>`. `pnpm build` verde (450KB). **Scoperto:** SessionMessages response è `Array<{ info: Message; parts: Part[] }>` (parti incluse); onEventType richiede explicit type param per narrowing TS. **Riprendere da:** Fase 2.2 — Tool-call card avanzata (o 2.3 HITL)._
- _2026-06-26 — **Fatto:** Fasi 1.2–1.6. 1.2: documentato in `docs/02-provider-setup.md` (da eseguire manualmente in locale). 1.3: Rust sidecar (`src-tauri/src/sidecar/mod.rs`: spawn/kill/health su porta libera; lib.rs: comandi Tauri `get_opencode_url`, `stop_opencode`, evento `opencode-ready`). 1.4: SDK layer TS — `src/opencode/client.ts` (singleton), `session.ts` (TQ hooks: list/get/create/prompt/abort), `OpencodeProvider.tsx` (bootstrap + QueryClient). 1.5: `src/opencode/events.ts` (SSE stream via `.stream` AsyncGenerator, `onEvent`/`onEventType`). Store: `src/stores/session.store.ts` (Zustand persist). 1.6: ADR `docs/03-adr-integration-layer.md`. `pnpm build` verde (267KB). **Scoperto:** SDK va importato da `@opencode-ai/sdk/client` (non root — `server.js` usa `child_process`). **Riprendere da:** Fase 2 — Chat shell + streaming markdown (2.1)._
- _2026-06-26 — **Fatto:** Fasi 0.1–0.4. Struttura repo, licenza MIT, .gitignore. Scaffold Tauri 2 + React 19 + TS + Vite (pnpm). Tailwind v4 CSS-first (@tailwindcss/vite), Motion, Lucide, clsx/twMerge. Design token completi in `src/index.css` (palette forge + amber accent, font display/body/mono, spacing, radii, variabili shadcn). `src/lib/utils.ts` cn(). `App.tsx` hello Forgia. `pnpm build` verde. Cargo bloccato in env remoto (static.crates.io policy) → verificare localmente. **Riprendere da:** 0.5 (CI GitHub Actions) → poi 1.2 (provider test) → 1.3 (sidecar Rust)._
- _2026-06-26 — **Fatto:** Fase 1.1. Installato `opencode-ai@1.17.11`, avviato `opencode serve` (porta 4096), ispezionata l'OpenAPI 3.1 (181 op, 444 schemi). Aggiunti `CLAUDE.md` (protocollo di lavoro) e `docs/01-opencode-openapi-findings.md`. **Scoperto:** il rischio #1 (§15.2) è rientrato — token/costo sono nativi su `Session` e `AssistantMessage`, la finestra è in `Model.limit.context`, esiste `GET /session/{id}/context` per "cosa è in contesto", più eventi `compaction.*`/`context.updated`. Streaming via SSE `GET /event`; HITL via `/permission`. **Riprendere da:** confermare il piano Fase 0 (scaffold) → poi eseguire; in parallelo Fase 1.2 (provider di test via `opencode auth`)._
- _(prima sessione: deciso di costruire la GUI sopra OpenCode; riscritti `PROGETTO.md` e `CHECKLIST.md`; raccolte le risorse — OpenCode SDK, Onlook per la selezione visuale, shadcn/Magic UI/Aceternity per la UI. Prossimo passo: Fase 1.1 — avviare `opencode serve` e leggere l'OpenAPI per validare l'integrazione.)_

---

## 🎯 Decisioni da prendere (vedi `PROGETTO.md` §15)

- [x] **D1.** Strumentazione selezione visuale: `data-*` (Onlook) **vs** jsx-source/click-to-component → **scelto: React fiber `_debugSource` + postMessage** (vedi `docs/05-adr-visual-selection.md`); superato poi dal **proxy iniettante universale** (2026-07-02) che funziona senza plugin
- [x] **D2.** Monaco **vs** CodeMirror 6 → **scelto: Monaco** (`@monaco-editor/react`, DiffEditor in Fase 3.2)
- [x] **D3.** Quanto usare Magic UI/Aceternity → **scelto: nessuna dipendenza** — effetti hand-rolled con Motion per controllare `prefers-reduced-motion` (Fase 9.1)
- [x] **D4.** Come bundlare/versionare il binario `opencode` → **scelto: `externalBin` in overlay release** + fallback PATH in dev (Fase 11.1, `docs/06-adr-sidecar-bundling.md`)
- [x] **D5.** Nome definitivo del progetto → **kikkoCode**

---

## Fase 0 — Fondamenta

- [x] **0.1** Inizializza il repo (struttura `PROGETTO.md` §12), licenza, `.gitignore`
- [x] **0.2** Scaffold Tauri 2 + React 19 + TypeScript + Vite; "hello window" che builda su Windows _(cargo check richiede static.crates.io — verificare in locale; frontend build verde)_
- [x] **0.3** Setup Tailwind v4 + shadcn/ui + Motion + Lucide; tema dark-first _(shadcn: setup base + `cn()`; v4 CSS-first; `src/index.css`)_
- [x] **0.4** Definisci i **design token** (palette 4–6 hex, scala tipografica display/body/mono, spacing) — `PROGETTO.md` §10 _(inglobato in 0.3: palette forge-950…50 + amber accent, font display/body/mono, spacing, radii in `@theme` di index.css)_
- [x] **0.5** CI minima (build + lint + format) + cartella `docs/` per gli ADR _(`.github/workflows/ci.yml`: frontend lint+build+format check; rust job con webkit2gtk; `.prettierrc`)_

## Fase 1 — Integrazione col motore OpenCode ⭐ *(da fare per prima)*

- [x] **1.1** Installa OpenCode; avvia `opencode serve`; apri l'OpenAPI `/doc` e **mappa cosa espone** (sessioni, streaming, eventi tool, stato contesto) → nota in `docs/` _(→ `docs/01-opencode-openapi-findings.md`)_
- [x] **1.2** Configura un provider di test (DeepSeek o Gemini) _(superato: setup completo **dalla GUI** — `AddProviderKey` + comando Rust `set_provider_key` che inietta l'env var e riavvia il motore; DeepSeek verificato funzionante end-to-end il 2026-07-01)_
- [x] **1.3** Backend Rust: **spawn/kill di `opencode serve`** come sidecar (porta libera, health check, riavvio) _(→ `src-tauri/src/sidecar/mod.rs`, `lib.rs`; Tauri cmds: `get_opencode_url`, evento `opencode-ready`)_
- [x] **1.4** Frontend: connetti `@opencode-ai/sdk` (`createOpencodeClient`); **crea sessione + invia prompt + ricevi risposta** (round-trip minimo) _(→ `src/opencode/client.ts`, `session.ts`, `OpencodeProvider.tsx`, `src/stores/session.store.ts`)_
- [x] **1.5** **Streaming** dei token/eventi dal server alla UI (rendering progressivo) _(→ `src/opencode/events.ts`: SSE `.stream` AsyncGenerator, `onEvent`/`onEventType` bus)_
- [x] **1.6** ADR: confine e contratto del **layer di integrazione** (cosa passa dalla GUI al motore e viceversa) _(→ `docs/03-adr-integration-layer.md`)_

## Fase 2 — Chat & revisione (la conversazione)

- [x] **2.1** **Chat shell** + streaming markdown (riusa primitive agent-UI da `awesome-shadcn-ui`)
- [x] **2.2** **Tool-call card**: render delle azioni dell'agente (bash, edit, search…)
- [x] **2.3** **Approva/Rifiuta** modifiche e comandi (human-in-the-loop) + allow-list auto-approve
- [x] **2.4** Input prompt + toggle **Plan/Build** (mappato sul motore)
- [x] **2.5** Gestione sessioni (lista, nuova, ripresa) nella sidebar

## Fase 3 — Editor & diff

- [x] **3.1** File tree del progetto _(→ `src/opencode/file.ts` hooks; `src/features/filetree/FileTree.tsx`: lazy-load, git status A/M/D, icone per tipo, selezione; `App.tsx` ristrutturato: sidebar sinistra 256px con sessions (max 45%) + file tree (flex-1 sotto))_
- [x] **3.2** **Diff inline** con Monaco (o CodeMirror, D2) per le modifiche proposte _(→ `@monaco-editor/react` installato; `src/stores/file.store.ts`; `src/features/filetree/FileDiffPanel.tsx`: Monaco DiffEditor side-by-side con ricostruzione original via `reverseApplyPatch`; Monaco Editor read-only per file senza diff; pannello h-[42vh] sotto la chat in `App.tsx`)_
- [x] **3.3** Apertura file alla riga esatta _(→ `file.store.ts`: `selectedLine + openFile(path, line?)`; `FileDiffPanel`: `revealLine` con Monaco `deltaDecorations` on mount + `useEffect` su `selectedLine`; `ToolCallCard`: chip `ExternalLink path:line` cliccabile che chiama `openFile`)_

## Fase 4 — Terminale & anteprima web

- [x] **4.1** **xterm.js**: mostra l'output dei comandi eseguiti dal motore _(→ `@xterm/xterm` + addon fit/web-links; `stores/terminal.store.ts` (entries+dedupe), `stores/ui.store.ts` (bottom panel tab terminal/diff); `features/terminal/useTerminalEvents.ts` (capta ToolPart bash da SSE), `TerminalPanel.tsx` (xterm read-only, tema forge, ANSI colorato); `App.tsx` bottom panel con tab Terminal/Diff; toggle nell'header chat)_
- [x] **4.2** Rilevamento **dev server** (`localhost:PORT`) dall'output _(→ `features/terminal/detectDevServer.ts` (regex Vite/Next/CRA, normalizza 0.0.0.0→localhost); `stores/preview.store.ts` (detectedUrl/previewUrl/dismissed); detection in `useTerminalEvents` su output completed/error + metadata running; `features/preview/DevServerBanner.tsx` (banner ambra "Open preview"))_
- [x] **4.3** **Anteprima web** nella webview Tauri con auto-reload (HMR) _(→ `features/preview/PreviewPanel.tsx`: iframe verso `previewUrl`, toolbar (reload/URL editabile/open-external/close), HMR del dev server ricarica l'iframe da solo; terza colonna in `App.tsx` (w-1/2, border-l); ADR `docs/04-adr-web-preview.md`: iframe ora, webview WRY nativa poi per la selezione visuale Fase 5)_

## Fase 5 — Selezione visuale degli elementi ⭐ *(il pezzo custom più serio)*

- [x] **5.1** Studia l'architettura di **Onlook** (mappatura `data-oid` file+riga) → nota in `docs/` _(→ `docs/05-adr-visual-selection.md`: React fiber `_debugSource` vs data-* vs WRY; scelto fiber+postMessage)_
- [x] **5.2** Decidi la strategia (D1) e **strumenta il progetto in anteprima** (plugin/attributi DOM→sorgente) _(→ `features/preview/ForgiaInspectorPlugin.ts`: plugin Vite `forgiaInspector()` che inietta bridge script via `transformIndexHtml`)_
- [x] **5.3** **Modalità selezione**: hover-highlight + click → ricava `file:riga` _(→ script bridge: highlight overlay amber, tooltip file:riga, fiber walk; stores/selection.store.ts; PreviewPanel: pulsante Crosshair, message listener, ready/ping handshake)_
- [x] **5.4** **Traduzione in prompt**: comporre il messaggio per OpenCode dall'elemento selezionato + intento utente _(→ `features/preview/ElementCompose.tsx`: pannello tag+file:riga+HTML, input testo, `buildPrompt`, `useSendPrompt`)_
- [x] **5.5** Loop completo: clicco → descrivo la modifica → il motore edita → l'anteprima si aggiorna _(→ wired: click→store→ElementCompose→useSendPrompt→OpenCode→modifica file→HMR→iframe reload automatico)_

## Fase 6 — Context Inspector ⭐ *(il differenziatore)*

- [x] **6.1** Recupera lo **stato del contesto** dagli eventi/endpoint OpenCode (+ stima lato GUI dove serve) _(→ `opencode/context.ts`: `useContextMessages` raw fetch `/api/session/{id}/context`; `useProviders` per `model.limit.context`; `EventSessionCompacted` in `useChatEvents.ts` invalida cache)_
- [x] **6.2** **Conteggio token** + % budget rispetto alla finestra del modello _(→ `ContextInspectorPanel`: aggrega `AssistantMessage.tokens.input` dell'ultimo step come proxy del current context; `contextLimit` da `Provider.models[id].limit.context`; progress bar con colori soglia 65%/85%)_
- [x] **6.3** **Ripartizione** del contesto (system/skill, AGENTS.md, storia, tool result, file) _(→ sezione "In context" in `ContextInspectorPanel`: breakdown per ruolo (`user/assistant/system/tool/summary`) con barre proporzionali e conteggio; colori per tipo)_
- [x] **6.4** Visualizza **compattato/troncato** e **costo** cumulato (shadcn Charts) _(→ sezione "Session totals" in `ContextInspectorPanel`: griglia 3-colonne input/output/reasoning/cache-r/cache-w/cost; `fmtNum` (K/M) e `fmtCost` ($0.0000))_
- [x] **6.5** Pannello "cosa l'agente vede ora" (file/elementi in vista) _(→ `features/inspector/ContextInspectorPanel.tsx` completo; tab "Inspector" in bottom panel `App.tsx` (sempre visibile, non condizionale su selectedFilePath); `ui.store.ts`: `BottomTab` aggiornato con `"inspector"`)_

## Fase 7 — Funzioni del motore esposte nella GUI

- [x] **7.1** **Switcher provider/modello** (sopra la config OpenCode) + setup chiavi guidato _(→ `opencode/config.ts`: `useConfig`, `useUpdateConfig`, `useSetAuth`; `features/settings/ModelSwitcher.tsx`: dropdown con tutti i provider + modelli raggruppati, contesto K, check mark sul modello attivo, input inline chiave API per provider con `env[]`; pill nel header di ChatShell)_
- [x] **7.2** **Skill**: UI per vedere/abilitare/disabilitare le skill di OpenCode _(→ `useAgents()` → `GET /agent`; tab "Agents & Skills" in `SettingsModal.tsx`: lista agenti con badge built-in/mode/model, tool abilitati; no enable/disable API esposta dall'SDK)_
- [x] **7.3** **MCP**: UI per aggiungere/configurare/monitorare server MCP _(→ `useMcpStatus()` → `GET /mcp`; `useConfig()`+`useUpdateConfig()`; tab "MCP Servers" in `SettingsModal.tsx`: lista server con status connected/disconnected, toggle enabled (via config.update), rimozione, form "Add local/remote"; `SettingsModal` aperto da gear icon nell'header)_
- [x] **7.4** **Subagenti**: visualizzazione quando il motore delega sotto-task _(→ `useSessionChildren(sessionId)` in `session.ts`; `SubagentList` component in `SessionSidebar.tsx`: sessioni figlie indentate sotto il parent attivo con icona Bot e pulsante amber; `useChatEvents.ts`: handler `session.created` invalida lista + children del parent)_

## Fase 8 — Differenziatori UX

- [x] **8.1** **Checkpoint timeline** visuale + rewind (sopra /undo–/redo) _(→ `session.ts`: `useRevertSession`, `useUnrevertSession`; `features/checkpoints/CheckpointTimeline.tsx`: timeline verticale `border-l`, dot amber sul punto revert, banner alert con "Restore", elementi successivi al revert con `opacity-40`; tab "Timeline" in `App.tsx` bottom panel; `session?.revert?.messageID` come indicatore di stato)_
- [x] **8.2** **Command palette** (Raycast-style), tutto da tastiera _(→ `features/commandpalette/CommandPalette.tsx`: overlay `Ctrl+K`/`Cmd+K` (listener globale in `App.tsx`), gruppo Actions (New Session / Toggle Terminal / Open Inspector / Open Timeline / Open Web Preview / Open Settings / Switch Model) + gruppo Sessions (ultime 12 top-level per updated); ricerca fuzzy su label+description+group; nav ↑↓ Enter Esc; `commandPaletteOpen`/`openCommandPalette`/`closeCommandPalette` in `ui.store.ts`)_
- [x] **8.3** **Cost & token meter** sempre visibile in barra di stato _(→ `features/statusbar/StatusBar.tsx`: strip 24px sempre visibile sotto il bottom panel in `App.tsx`; progress bar context window con %, token/limit (fmtNum K/M); color coding verde/amber/rosso su soglie 65%/85%; contatore passi/msgs in ctx; costo cumulato sessione `$X.XXXX`; pill `provider/model` a destra)_

## Fase 9 — Pass estetico (la UI spettacolare)

- [x] **9.1** Momenti-firma con **Magic UI/Aceternity** (onboarding, empty state, transizioni) — con misura (D3) _(→ `features/onboarding/WelcomeScreen.tsx`: empty state firmato con anvil + forge-glow ambra pulsante (Motion), stagger reveal di titolo/sottotitolo/chip, 3 suggestion-chip "Plan a feature / Explain this codebase / Find a bug" che prefillano un prompt via `onPrompt`; hint Ctrl+K. **D3:** niente dipendenze Magic UI/Aceternity — effetto hand-rolled con Motion (già in stack) per controllare reduced-motion a mano. Wired in `ChatShell` al posto dell'empty state inline)_
- [x] **9.2** Tipografia, spacing, micro-interazioni rifiniti; tema chiaro _(→ `stores/theme.store.ts`: tema persistito in localStorage, default dark con rispetto di `prefers-color-scheme: light`, applica `.light` su `<html>` + `colorScheme`; import side-effect in `main.tsx` per no-FOUC; `features/settings/ThemeToggle.tsx`: Sun/Moon nell'header ChatShell; `index.css`: blocco `.light` completato (sidebar, ring, destructive, ombre morbide) + scrollbar light-aware + var `--theme-transition`)_
- [x] **9.3** Sequenza d'apertura + reveal della chat (Motion) _(→ `App.tsx`: root `motion.div` con fade-in 0.5s una-tantum al mount; `features/chat/MessageList.tsx`: ogni bubble entra con fade-up 0.25s (key stabile `info.id` → non ri-anima durante lo streaming); tutti gli effetti saltano con `useReducedMotion()`)_

## Fase 10 — Hardening

- [x] **10.1** `prefers-reduced-motion` ovunque (specie Magic UI/Aceternity) + focus tastiera + contrasto _(→ audit: `:focus-visible` globale già in `index.css`; `@media (prefers-reduced-motion)` copre tutte le classi Tailwind `animate-*`; `useReducedMotion()` su tutte le animazioni Motion JS (App, MessageList, WelcomeScreen) — nessuna animazione non-gated. `role="dialog"`/`aria-modal`/`aria-label` su CommandPalette e SettingsModal; `aria-label` sul Close icon-only)_
- [x] **10.2** Stati di errore/empty curati (sidecar caduto, provider non configurato, dev server assente) _(→ `features/statusbar/SidecarStatusBanner.tsx`: strip globale "Connecting…" al boot / "OpenCode engine disconnected" + pulsante Reconnect su crash; ChatShell copy aggiornato per puntare al banner; dev-server assente già coperto da `DevServerBanner`; empty state curato dal `WelcomeScreen`)_
- [x] **10.3** Resilienza del sidecar (crash/restart) e gestione porte occupate _(→ `src-tauri/src/sidecar/mod.rs`: `start` con retry su porta fresca (3 tentativi, gestisce TOCTOU/porte occupate) + errore chiaro se `opencode` non è nel PATH; `restart`, contatore `generation`, `is_healthy`; `src-tauri/src/lib.rs`: `spawn_health_monitor` (poll /health ogni 3s + re-check, emette `opencode-error` se cade) con generation-guard; comando `restart_opencode`; `src/opencode/sidecar.ts`: `restartSidecar()` invoca il comando e resetta lo stream. ⚠️ Rust non compilabile in web-env (policy blocca `static.crates.io`) → verificato per ispezione, CI compila)_
- [x] **10.4** Test del layer di integrazione (sessioni, streaming, approvazioni) _(→ Vitest (jsdom) + script `test`/`test:watch` + `vitest.config.ts`; 22 test: `chat.store` (reducer streaming), `permission.store` (auto-allow per tipo/pattern/array), `detectDevServer` (forme URL), `events` (isEventType), `theme.store` (classe .light/persistenza). CI: step `pnpm test` aggiunto al job frontend)_

## Fase 11 — Packaging & release

- [x] **11.1** **Bundla il binario `opencode`** nel sidecar (D4) + build/installer Windows (Tauri) _(→ `externalBin ["binaries/opencode"]` nell'overlay `tauri.release.conf.json` (mergiato in CI con `tauri build --config`, NON nel config base — altrimenti `cargo check`/dev falliscono perché Tauri valida l'esistenza del binario non committato); `sidecar::opencode_bin()` preferisce il binario accanto all'eseguibile, fallback su PATH in dev; `src-tauri/binaries/` README (naming target-triple) + `.gitignore` (binario non versionato, fetch per-target a build time); `.github/workflows/release.yml` (build Windows via tauri-action su tag `v*`, fetch sidecar con suffisso msvc); `docs/06-adr-sidecar-bundling.md` (ADR D4). ⚠️ bundle non verificabile in web-env, vedi nota Fase 10)_
- [x] **11.2** Onboarding al primo avvio: configura il primo provider, spiega Plan/Build, mostra l'Inspector _(→ `stores/onboarding.store.ts` (flag `completed` persistito + reset); `features/onboarding/OnboardingWizard.tsx`: wizard 3 step (welcome → connect provider con key-setup riusando useProviders/useSetAuth + auto-default model → tour Plan/Build + Context Inspector); mostrato in `App.tsx` al primo avvio quando l'engine è ready; azione "Replay Intro" in CommandPalette)_
- [x] **11.3** README + note di versione _(→ `README.md` (overview, differenziatori, architettura, stack, dev setup, packaging, status) + `CHANGELOG.md` (Keep a Changelog, sezione Unreleased con tutte le feature Fasi 1–11))_
- [ ] **11.4** _(futuro)_ macOS/Linux

## Fase 12 — Evoluzione post-roadmap (luglio 2026)

> Lavoro oltre la roadmap originale, guidato dall'uso reale su Windows.

- [x] **12.1** **Fix connessione motore su Windows** (shim npm `.cmd` via `cmd /C`, PATHEXT, `OPENCODE_BASE_URL`, health probe no-proxy) + fix race listener/poll `opencode-ready`
- [x] **12.2** **Provider key dalla GUI** (env-var injection + restart motore; test chiave; selezione modello; selettore reasoner⇄chat; pallino online col modello attivo)
- [x] **12.3** **Streaming reale + 20× più veloce** (niente refetch per token; merge storia+live; fix shape SDK 0.15 vs engine 1.17 via `messageShape.ts`)
- [x] **12.4** **Sistema Skills** (catalogo 12 skill design, matcher keyword+descrizione, auto-inject con badge, Skills Manager con trigger words visibili, chip live "will apply")
- [x] **12.5** **Integrazioni chat**: diagrammi Mermaid, tool-call card live parallele, Plan Tree (todo del motore in tempo reale), Cost & Context Guard pre-invio
- [x] **12.6** **Project/workspace picker** ⭐ (apri cartella / clona repo GitHub / crea progetto; recenti; riapertura ultimo progetto; barra project + apri-in-Explorer)
- [x] **12.7** **Pannello inferiore ridimensionabile** (maniglia trascinabile) + tasto anteprima in-app
- [x] **12.8** **Anteprima automatica universale**: server statico integrato, dev server gestito da kikkoCode (output catturato → URL reale), probe IPv4+IPv6 su porte realmente in ascolto, URL dall'output = fonte di verità, auto-open a fine task, watch persistente
- [x] **12.9** **Selezione elementi senza plugin** ⭐ (preview server = reverse-proxy iniettante; inspector universale: fiber file:riga quando c'è, sempre selettore CSS+HTML; ElementCompose anche senza source-map)
- [x] **12.10** **Fix "stuck on Working"** (running flag posseduto dalla finestra send→idle, non da `session.updated`)
- [x] **12.11** **Pacchetto affidabilità** (SSE auto-reconnect con backoff; memoria chat riconciliata su idle; auto-scroll solo se già al fondo) + 8 test sul matcher skills (30 totali)
- [x] **12.12** **Error Radar** ⭐ (radar iniettato: errori js/promise/resource/console → badge ⚠ + drawer + "Fix with agent" in un click)
- [x] **12.13** **Review-diff con approvazione per file** (`ReviewPanel` in chat: file toccati con badge A/M/D e +/−, click → diff Monaco, Discard a due step per file via `discard_file_changes` — git checkout / delete per i nuovi; refresh su idle)
- [x] **12.14** **Coda di task** (`queue.store` per sessione; invio durante il run = accoda; a idle parte il prossimo; chip "queue · n" sopra l'input con rimozione; Enter/bottone ⊞ durante il run)
- [x] **12.15** **Notifiche desktop** a fine task (`tauri-plugin-notification`; solo se la finestra non è a fuoco; permesso lazy)
- [x] **12.16** **Screenshot → self-critique** (bottone 📷: `capture_preview` via Edge/Chrome headless → PNG allegato come FilePart a un prompt di critica visiva; `useSendPrompt.files`; richiede modello con visione — auto a fine task = futuro)
- [x] **12.17** **HMR attraverso il proxy** (niente WS nel proxy: lo script iniettato ridireziona i WebSocket della pagina dritti al dev server via `__kikko_ws_target__` — Vite/Next HMR live nel pannello)
- [x] **12.18** **Kill pulito dei processi su Windows** (`process::kill_child_tree`: `taskkill /T /F` sull'albero prima del kill diretto; usato da Sidecar e DevRunner — niente più node orfani con la porta occupata)
- [x] **12.19** **Perf chat** (MessageBubble `memo` + WeakMap per `parts` stabili: in streaming si ri-renderizza solo il bubble attivo; windowing completo = futuro se servirà)
- [x] **12.20** **Autopilot con budget** 🚀 (toggle Auto nell'input con `$ budget` e `× iter`; controller su session.idle: marker AUTOPILOT_DONE / fuel gauge dai cost del motore / cap iterazioni; AutopilotBar con stop; la coda attende)
- [x] **12.21** **Memoria persistente di progetto** 🧠 (blocco marcato in `AGENTS.md` — iniettato nativamente dal motore; distillazione su idle via sessione nascosta plan-mode con merge/dedupe/cap 4K; scrittura deterministica Rust; throttle + Memorize now + toggle; sessioni `[kikko]` filtrate/silenziate; fix leak live-messages cross-sessione)
- [x] **12.22** **Rules tab + import skill da URL** (editor AGENTS.md in Settings; `fetch_text` https-only + parser markdown → skills custom persistite, catalogo dinamico built-in+custom, rimozione 🗑)
- [ ] **12.23** **Test Radar** (rileva il test runner del progetto, Run tests, parse fallimenti → badge + "Fix with agent" — pattern Error Radar)

---

## ✅ Definition of Done per fase

Builda, i suoi test passano, è spuntata qui, e il Log di sessione è aggiornato.

---

*Riferimento architetturale completo: `PROGETTO.md`.*
